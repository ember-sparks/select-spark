{{#spark-page
  config=config
}}

  {{select-spark
    title=demo.title
    selected=demo.selected
    options=options
    disabled=demo.disabled
    onChange=(action (mut demo.selected))
  }}

{{/spark-page}}
